
To compile the project:
  make

To run tests (they are ran during compilation/packaging):
  make test

To clean the  current working directory:
  make clean

To execute the example (after compilation):
  java -jar target/rectangle-intersection-detection-1.0-SNAPSHOT-jar-with-dependencies.jar src/test/resources/sample.json
